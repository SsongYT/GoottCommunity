<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.goott.mappers.questionBoardMapper">
	
	<select id="getTotalPostCnt"
		resultType="java.lang.Integer">		
		SELECT count(*) FROM question_board;
	</select>

	<select id="getAllBoard"
		resultType="com.goott.vodto.ksh.QuestionBoardDto">		
		SELECT * FROM question_board ORDER BY NO DESC
	</select>

	<insert id="insertBoard"
		parameterType="com.goott.vodto.ksh.QuestionBoardDto"
		useGeneratedKeys="true" keyProperty="no">
		INSERT INTO question_board (writer, title, content, category)
		VALUES(#{writer}, #{title}, #{content}, #{category})
	</insert>

	<insert id="insertUploadFiles">
		INSERT INTO upload_file
		(extension, original_fileName,
		new_fileName, file_size, board_no, board_category_no)
		VALUES
		<foreach item="item" index="index" collection="list"
			separator=",">
			(#{item.extension}, #{item.original_fileName},
			#{item.new_fileName},#{item.file_size},#{no},1)
		</foreach>
	</insert>
	
	<select id="getDetailBoard"
		resultType="com.goott.vodto.ksh.QuestionBoardDto">
		SELECT q.*, count(DISTINCT u.uf_no) AS ufNoCount, COUNT(DISTINCT a.answer_no) AS answerCount FROM question_board q LEFT JOIN upload_file u
		on q.no = u.board_no and u.board_category_no = 1
        LEFT JOIN answers a ON q.no = a.ref and a.board_category_no = 1 where q.no = #{no};  		
	</select>
	
	<select id="getBoardUploadFile"
		resultType="com.goott.vodto.ksh.UploadFiles">
		SELECT * FROM upload_file WHERE board_no = #{no}
	</select>
	
	<select id="getAllAnswers"
		resultType="com.goott.vodto.ksh.Answers">
		SELECT * FROM answers WHERE ref = #{no} and board_category_no = 1;
	</select>
</mapper>